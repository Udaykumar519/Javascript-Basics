//Mostly we use let and const keywords for variables in JS and React.
// v8 engine scans all the variables make it as undefined value and functions, stores it in memory.
console.log(b());

var a=10;
console.log(a);

function b() {
    return 1;
}
